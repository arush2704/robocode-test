# Word Ladder Problem Documentation

## Overview
This document describes the implementation and usage of the `wordLadderLength` function in `Evaluation.java`, which solves the word ladder problem using Breadth-First Search (BFS).

## Function Signature
```
public static int wordLadderLength(String beginWord, String endWord, List<String> wordList)
```
- **Input:**
  - `beginWord` (String): The starting word.
  - `endWord` (String): The target word.
  - `wordList` (List<String>): The list of allowed words.
- **Output:**
  - `int`: The length of the shortest transformation sequence, or 0 if no such sequence exists.
- **Throws:**
  - `IllegalArgumentException` if any argument is null.

## Algorithm
- Uses BFS to explore all possible word transformations level by level.
- At each step, changes one letter at a time and checks if the new word is in the word list and not visited.
- Returns the number of steps when the end word is reached, or 0 if unreachable.

## Example Usage
```
String begin = "hit";
String end = "cog";
List<String> wordList = Arrays.asList("hot","dot","dog","lot","log","cog");
int result = Evaluation.wordLadderLength(begin, end, wordList); // result is 5
```

## Complexity
- **Time Complexity:** O(N * M^2), where N is the number of words and M is the word length.
- **Space Complexity:** O(N * M) for the queue, visited set, and word set.

## Edge Cases
- No possible transformation: Returns 0.
- beginWord equals endWord: Returns 1 if endWord is in wordList.
- Case sensitivity: The function is case-sensitive.
- Null input: Throws `IllegalArgumentException`.

## Testing
See `EvaluationWordLadderTest.java` for comprehensive test cases, including:
- Basic transformation, no possible transformation, begin equals end, case sensitivity, null input, single-letter words, and disconnected graphs.

## Author
Generated by GitHub Copilot

